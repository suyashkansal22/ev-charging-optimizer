# Simulator (Person 4) — grid sim + price ML

Day 1: the API reads static fixtures in `api/seed/*.json`.
Later, this service generates live StationState + Forecast and POSTs them to the API.
Keep outputs in the contract shape (contracts/schemas.md).
